Robot se zotavil po předchozí chybě a je znovu schopen fungovat.

Robot byl pozastaven od <@datetime data.since /> do <@datetime data.until />.
Celková doba pozastavení robota: ${data.days} d., ${data.hours} hod., ${data.minutes} min. a ${data.seconds} s.

