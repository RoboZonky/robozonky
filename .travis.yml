language: java
env:
  global:
    - JAVA_OPTS=-Xmx2g
    - MAVEN_OPTS=$JAVA_OPTS
addons: # make sure we're using the latest Java as Travis default is quite old
  apt:
    packages:
      - oracle-java8-installer
      - oracle-java9-installer
os:
  - linux
jdk:
  - oraclejdk8
  - oraclejdk9
env: # split execution per-module so that PITest doesn't cause the build to timeout
  - MODULE=robozonky-api
  - MODULE=robozonky-common
  - MODULE=robozonky-integration-zonkoid
  - MODULE=robozonky-notifications
  - MODULE=robozonky-strategy-natural
  - MODULE=robozonky-app
  - MODULE=robozonky-installer
cache:
  directories:
  - $HOME/.m2
install:
  - mvn --batch-mode -T 1.5C clean install -DskipTests -Dgpg.skip
script:
  - cd $MODULE
  - mvn --batch-mode install -Dassembly.skipAssembly -Dgpg.skip
